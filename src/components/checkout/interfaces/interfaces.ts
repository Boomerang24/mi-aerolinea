export interface CheckoutCardProps {
  originCity: string;
  destinationCity: string;
  departureDate: string;
  flightHour: string;
  ticketPrice: number;
  ticketCounter: number;
  returnDate?: string;
}
